# סיכום: חיבור הבוט לאינטראקטיב ברוקרס (IBKR)

## מה הושג
הבוט (בתיקייה `מנהל ההשקעות שלך 💼`) מחובר בהצלחה ל-**Interactive Brokers Paper Trading** (חשבון דמו).

## פרטי החשבון
- **מספר חשבון דמו:** DUQ938552
- **שם משתמש להתחברות:** tobarb632 (⚠️ לא tobarb236 — זו טעות הקלדה נפוצה)
- **יתרה:** $1,000,000 וירטואלי
- **פורטים:** 4002 = Paper (דמו), 4001 = Live (אמיתי) — ב-IB Gateway, לא TWS

## התשתית
- **IB Gateway** מותקן ב-`C:\Users\תומר\Jts-Gateway\ibgateway.exe`
- חייב להיות **פתוח ומחובר** כדי שהבוט יעבוד על IBKR
- **חובה להתחבר ידנית כל פעם** — סיסמה + אישור בטלפון (2FA). אין דרך לעקוף את זה.
- מתנתק אוטומטית פעם ביום (23:45) — הוגדר "Auto Restart" כדי שינסה להתחבר לבד, אבל לא בטוח שעוקף את ה-2FA
- `.env`: `ACTIVE_BROKER=ibkr`, `IBKR_HOST=127.0.0.1`, `IBKR_PORT=4002`

## באגים שתוקנו בקוד (`broker_ibkr.py`, `test_ibkr_connection.py`)
1. **באג מטבע:** הקוד קרא רק USD, אבל חשבון ה-IBKR ב-ILS (שקלים) — הראה $0 בטעות. תוקן לבחור BASE→USD→כל מטבע זמין.
2. **באג event loop:** `ib_insync` נכשל עם thread pool משותף. תוקן לחיבור thread-local, event loop טרי בכל ניסיון, וניסיון חוזר אוטומטי.

## מצב נוכחי
- ✅ הבוט **מחובר** ל-IBKR Paper, קורא יתרה נכון
- ❌ **עדיין לא ראינו עסקה אחת בפועל** מבוצעת על IBKR (רק חיבור אומת)
- 🔴 **לא מחובר ל-Live** ולא מתוכנן להתחבר בקרוב — המשתמש עדיין בשלב הוכחת הדמו

## פיצ'רים חדשים שנוספו לטלגרם
- **`/buy TICKER`** — קנייה ידנית (דורשת `confirm` שני, או `/autobuy N` להפעלת אישור עומד עד 14 יום)
- **`/sell TICKER`** — כבר היה קיים
- שווי תיק + רווח מצטבר מאז ההתחלה מוצג בכל הודעת מכירה

## התוכנית להמשך (מוסכמת עם המשתמש)
1. לתת ל-IBKR Paper לרוץ שבוע-שבועיים, לוודא עסקאות תקינות
2. לעבור יחד על מנגנוני הגנה (סטופ-לוס, הגבלת הפסד יומי, כפתור חירום)
3. רק אז — לשקול מעבר ל-Live, בסכום קטן שהמשתמש יבחר
4. `MAX_BUDGET=10000` כבר מוגדר ב-`.env` (כרגע חל על הסימולציה)

## גבול קבוע (לא משתנה לעולם)
קלוד **לעולם לא מבצע קנייה/מכירה בעצמו** — לא בדמו, לא ב-Live, גם עם הרשאה מפורשת. הבוט (קוד) הוא זה שמבצע פעולות, אוטומטית, ללא אישור לכל עסקה בודדת. קלוד גם **לא מקליד סיסמאות** — ההתחברות ל-Gateway תמיד נעשית ע"י המשתמש בעצמו.
