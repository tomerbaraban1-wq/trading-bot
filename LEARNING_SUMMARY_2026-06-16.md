# סיכום הלמידה של הבוט — 16.6.2026

חולץ מ-`data/trading.db` (16 טבלאות). זה "מה שהבוט למד" עד היום.

> ⚠️ הערה חשובה: עד היום הבוט החליט גם על **נתונים שבורים** (באג ה-yfinance 401 שתיקנּו ב-16.6). לכן חלק מהלמידה מבוססת על נתונים לא אמינים — ספירת ה"הוכחה" האמיתית מתחילה מעכשיו, על נתונים נקיים.

## 1. הפרמטרים שהבוט כייל לעצמו (`learning_thresholds`)
הברכיים שהבוט כוונן לבד (עודכן לאחרונה 16.6 17:57):

| פרמטר | ערך | משמעות |
|---|---|---|
| `min_rsi` | 35 | לא לקנות אם RSI נמוך מדי (oversold קיצוני) |
| `max_rsi` | 72 | לא לקנות overbought |
| `max_bb_position` | 0.9 | לא לקנות מעל 90% מרוחב בולינגר |
| `min_volume_ratio` | 0.8 | דרוש ווליום של לפחות 80% מהממוצע |
| `min_sentiment` | 5.0 | ציון סנטימנט מינימלי לכניסה |

## 2. למידה מ-Backtesting (`backtest_results`, 145 ריצות)
- אחוז הצלחה ממוצע: **~39.5%**
- תשואה ממוצעת לסיגנל: ~0.99%
- "ציון אופטימלי" לכניסה שהתכנס אליו: **~51**
- `insights`: ריקים (`[]`) — הבוט לא הפיק תובנות מילוליות, רק מספרים.

## 3. עסקאות אמיתיות שנסגרו (`trade_log`)
- **11 עסקאות סגורות** | 3 רווחיות (27%) | רווח נטו **+$656** (~+6.6% על 10k וירטואלי)
- ⚠️ מדגם זעיר — 11 עסקאות לא מספיקות לשום מסקנה סטטיסטית.

## 4. תנאי שוק שהבוט חווה (`market_scenarios`)
- שוק שטוח (flat): **12**
- ראלי / עולה (rally): **2**
- ⚠️⚠️ **הבוט מעולם לא חווה שוק יורד או משבר.** זו הנקודה הקריטית ביותר לגבי מוכנות לכסף אמיתי.

## 5. בעיית איכות נתונים שזוהתה (`error_patterns`)
- 534 שורות, אבל רובן **כפילויות זהות** של `unclassified_loss` עם `severity` מוזר (261.94) ו-`fix=Review manually`.
- נראה כמו באג ברישום הטעויות — שווה ניקוי/תיקון בהמשך (לא דחוף).

## בשורה תחתונה
מה שהבוט "למד" = בעיקר **כיול פרמטרים** + סטטיסטיקת backtest. אבל:
1. מדגם העסקאות האמיתיות זעיר (11).
2. הוא ראה רק שוק שטוח ועולה — **אפס ניסיון בירידות**.
3. חלק מהלמידה התבססה על נתונים שבורים (תוקן היום).

→ בדיוק למה צריך עוד חודשים של ריצה **נקייה** לפני שחושבים על כסף אמיתי.
